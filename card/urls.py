from django.urls import path, include
from . import views




app_name = 'card'
urlpatterns = [
    # path('show-card/', views.CardListApiview.as_view(), name='show-card'),
    # path('card-create/', views.CardCreateApiView.as_view(), name='card-create'),
    # path('card-show-update/<pk>/', views.CardShowUpdate.as_view(), name='card-show-update'),
]
